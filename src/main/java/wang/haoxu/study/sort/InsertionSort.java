package wang.haoxu.study.sort;

/**
 * @Description TODO
 * @Date 2021/8/5 3:19 下午
 * @Author william.wang
 */
public class InsertionSort {
    /**
     * 外层代表已经排好的最大索引（默认第一个排好了）
     * 内层为候选插入点
     * @param arr
     */
    private static void sort(int[] arr) {
        if (arr == null || arr.length < 2) {
            return;
        }
    }
}
