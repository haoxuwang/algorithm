package wang.haoxu.study.sort;

/**
 * @Description TODO
 * @Date 2021/8/5 3:19 下午
 * @Author william.wang
 */
public class BubbleSort {
    /**
     * 外层代表比较到最后一个的索引（每一遍谁是最后一个）
     * 内层为两数比较时的前一个
     * @param arr
     */
    private static void sort(int[] arr) {
        if (arr == null || arr.length < 2) {
            return;
        }
    }
}
